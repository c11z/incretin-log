Deploy the full stack. Run these steps in order, stopping if any fails:

1. From `site/`, run `npm run typecheck`. If there are errors, fix them before continuing.
2. From `site/`, run `npm run build`. Confirm the output looks correct.
3. From `worker/`, run `npm run deploy`. Report the deployed URL.

After all steps complete, summarize what was deployed and whether anything needed fixing.
