Start the local development environment:

1. From `site/`, start `npm run serve` in the background.
2. From `worker/`, start `npm run dev` in the background.

Report both URLs:
- Frontend: http://localhost:3000
- Worker API: http://localhost:8787
